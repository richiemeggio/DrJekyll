# .github/workflows/render-keeper.yml
name: Render Keeper

on:
  schedule:
    - cron: '*/10 8-22 * * *'  # Ogni 10 minuti dalle 8:00 alle 22:59 UTC (9-23 italiane)
  workflow_dispatch:            # Permette avvio manuale

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          echo "🕒 $(date)"
          echo "📡 Invio ping a Render..."
          if curl -s -f https://drjekyll-3v7d.onrender.com; then
            echo "✅ Ping riuscito"
          else
            echo "❌ Ping fallito"
            exit 1
          fi
