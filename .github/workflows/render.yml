# .github/workflows/render-keeper.yml
name: Render Keeper

on:
  schedule:
    - cron: '* 9-23 * * *'  # Ogni minuto dalle 9:00 alle 23:59

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: |
          echo "🕒 $(date)"
          echo "📡 Invio ping a Render..."
          if curl -s -f https://tuo-service.onrender.com; then
            echo "✅ Ping riuscito"
          else
            echo "❌ Ping fallito"
            exit 1
          fi
